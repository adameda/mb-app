# Copier ce fichier en .env et remplir avec vos vraies valeurs

# === FLASK ===
# Générer une clé secrète forte : python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=changez-cette-cle-secrete-par-une-vraie-cle-aleatoire

# Environnement : development ou production
FLASK_ENV=production
FLASK_DEBUG=False

# === BASE DE DONNÉES ===
# Configuration PostgreSQL (pour Docker Compose)
POSTGRES_DB=devis_app
POSTGRES_USER=devis_user
POSTGRES_PASSWORD=votre_password_postgres_local

# URL de connexion
# Développement local (SQLite)
DATABASE_URL=sqlite:///devis.db

# Docker Compose (PostgreSQL)
# DATABASE_URL=postgresql://devis_user:votre_password@localhost:5432/devis_app

# Production (PostgreSQL - fourni automatiquement par Railway)
# DATABASE_URL=postgresql://user:password@host:5432/database

# === AUTHENTIFICATION ===
# Identifiants de l'utilisateur admin de l'app
ADMIN_USERNAME=admin
# Générer le hash
ADMIN_PASSWORD_HASH=pbkdf2:sha256:600000$VotreHashIci

